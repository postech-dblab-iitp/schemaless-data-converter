title: test
table_name: lineitem
schema_file: ./schemas/tpch/lineitem.yaml
data_file: ./data/tpch
output_path: ./results
partitions: [p1, p2]
p1:
  portion: 50
  column_removing:
    activate: false
  type_conversion:
    activate: false
p2:
  portion: 50
  column_removing:
    activate: true
    columns: [L_LINESTATUS]
  type_conversion:
    activate: true
    columns: [L_QUANTITY]
    types: [VARCHAR(20)]